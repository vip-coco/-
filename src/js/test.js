  // eslint-disable-next-line
  export default function mul(x, y) {  return x * y; }  
  // eslint-disable-next-line 
  export default function count(x, y) {return x - y;}
    // eslint-disable-next-line
  console.log(count(8, 1),mul(8, 1));
  